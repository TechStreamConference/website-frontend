export enum LoginState {
    Login,
    ResetPasswordPending,
    ResetPasswordSuccess,
}

export enum SingleProcessState {
    Default,
    Success,
}

export enum UserProfileState {
    None,
    Mail,
    Password,
    Name,
}

export enum ApprovalTalkType {
    Pending,
    Tentative,
}

export enum ApprovalSpeakerTeamMemberType {
    Speaker,
    TeamMember,
    SocialMedia,
}
