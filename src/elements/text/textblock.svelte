<p><slot /></p>

<style>
	p {
		font-size: 1.5rem;
		font-family: gnuolane, sans-serif;
		font-weight: 500;
		font-style: normal;
		color: var(--text-color);
		text-wrap: wrap;
		margin: 0 auto;
		text-align: var(--text-align);
		width: var(--width);
	}
</style>
